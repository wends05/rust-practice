{
  "name": "@rust-practice/api",
  "private": true,
  "scripts": {
    "build": "cargo build --release",
    "dev": "cargo run",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets -- -D warnings",
    "format": "cargo fmt",
    "format:check": "cargo fmt --check",
    "clean": "cargo clean"
  }
}
